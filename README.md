# SHA Flood Bot 🤖

Telegram бот для групповых чатов с функционалом для организаторов и участников.

## 🚀 Быстрый старт

### Запуск через Docker (рекомендуется)

#### 1. Подготовка

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_бота_от_BotFather

# Настройки PostgreSQL (для Docker)
POSTGRES_DB=vshu_db
POSTGRES_USER=vshu
POSTGRES_PASSWORD=1234
POSTGRES_HOST=db
POSTGRES_PORT=5432

ADMIN_IDS=[]
```

#### 2. Подготовка Excel файла

Разместите файл с организаторами в корне проекта с именем `organizers.xlsx`.

**Требования к Excel файлу:**
- Первая строка - заголовки (будет пропущена)
- Колонки должны быть в следующем порядке:
  1. ФИО (обязательно)
  2. Подразделение (обязательно)
  3. Телеграмм юзернейм
  4. Дата рождения (формат: 18.06.2004)
  5. Факультет
  6. Курс обучения
  7. Учебная группа (формат: ШАЦ25-1)
  8. Номер телефона (формат: 8 (999) 123-45-67)
  9. Есть ли водительские права и машина
  10. Ближайшие станции метро
  11. Адрес проживания

#### 3. Запуск

```bash
docker-compose up -d --build
```

Бот автоматически:
- ✅ Дождется запуска PostgreSQL
- ✅ Применит миграции базы данных
- ✅ Загрузит данные из `organizers.xlsx` (если файл существует)
- ✅ Запустит бота

#### 4. Просмотр логов

```bash
docker-compose logs -f bot
```

#### 5. Остановка

```bash
docker-compose down
```

---

### Локальная разработка (без Docker)

#### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

#### 2. Настройка

Создайте файл `.env`:

```env
BOT_TOKEN=ваш_токен_бота_от_BotFather
DB_URL=sqlite+aiosqlite:///./bot.db
ADMIN_IDS=[]
```

#### 3. Инициализация базы данных

```bash
python migrate.py
```

#### 4. Загрузка данных организаторов

```bash
python utils.py load organizers.xlsx
```

#### 5. Запуск бота

```bash
python main.py
```

## 📋 Функционал

### Инициализация чата

При добавлении бота в групповой чат, он спрашивает: **"Че за чат?"**

Доступны два варианта:
- 👥 **Участники** - функционал для участников мероприятий
- 🎯 **Организаторы** - функционал для команды организаторов

### Команды для чата организаторов

#### `!фамилия <фамилия>`
Поиск организатора по фамилии в базе данных.

**Примеры:**
```
!фамилия Иванов
!фамилия Петр
```

**Что показывает:**
- ФИО
- Подразделение
- Telegram username
- Телефон
- Дата рождения
- Факультет, курс, группа
- Наличие авто/прав
- Ближайшее метро
- Адрес (если есть)

Если найдено несколько человек - показывает список для уточнения.

### Команды администратора

#### `/chat_info`
Показывает информацию о текущем чате (тип, дата добавления).
Доступна только администраторам чата.

## 🗄️ Структура базы данных

### Таблица `chats`
Хранит информацию о чатах, в которые добавлен бот:
- `chat_id` - ID чата в Telegram
- `chat_type` - тип чата ('organizers' или 'participants')
- `chat_title` - название чата
- `added_at` - дата добавления

### Таблица `users`
Хранит информацию об организаторах:
- `full_name` - ФИО (обязательно)
- `department` - подразделение (обязательно)
- `telegram_username` - username в Telegram
- `telegram_id` - ID пользователя в Telegram
- `birth_date` - дата рождения
- `faculty` - факультет
- `course` - курс обучения
- `study_group` - учебная группа
- `phone_number` - номер телефона
- `has_car` - наличие водительских прав и машины
- `nearest_metro` - ближайшие станции метро
- `address` - адрес проживания

## 🛠️ Дополнительные команды

### Очистка таблицы пользователей

```bash
python utils.py clear
```

⚠️ **Внимание:** Это удалит всех пользователей из базы данных!

## 📦 Docker

### Запуск через Docker Compose

```bash
docker-compose up -d
```

### Остановка

```bash
docker-compose down
```

## 🔧 Разработка

### Структура проекта

```
sha_flood_bot/
├── database/
│   ├── __init__.py
│   ├── engine.py       # Настройка SQLAlchemy
│   └── models.py       # Модели БД
├── handlers/
│   ├── __init__.py
│   ├── chat_init.py    # Инициализация чатов
│   ├── orgkom_handlers.py  # Обработчики для организаторов
│   ├── user_handlers.py    # Обработчики для участников
│   └── admin_handlers.py   # Обработчики для админов
├── config.py           # Конфигурация
├── main.py            # Точка входа
├── migrate.py         # Миграции БД
├── utils.py           # Утилиты (загрузка из Excel)
└── requirements.txt   # Зависимости
```

## 📝 TODO

- [ ] Функционал для чата участников
- [ ] Дополнительные команды для организаторов
- [ ] Система уведомлений
- [ ] Статистика использования

## 📄 Лицензия

MIT

