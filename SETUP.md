# 🚀 Быстрая инструкция по запуску

## Что уже настроено автоматически:

✅ **Миграции базы данных** - применяются автоматически при запуске контейнера  
✅ **Загрузка данных из Excel** - происходит автоматически, если файл `user_data.xlsx` существует  
✅ **PostgreSQL** - настроен в docker-compose.yml  
✅ **Монтирование Excel файла** - файл `user_data.xlsx` монтируется в контейнер  

---

## Что нужно сделать:

### 1. Создать файл `.env`

```env
BOT_TOKEN=ваш_токен_от_BotFather

# Настройки PostgreSQL (уже настроены в docker-compose.yml)
POSTGRES_DB=vshu_db
POSTGRES_USER=vshu
POSTGRES_PASSWORD=1234
POSTGRES_HOST=db
POSTGRES_PORT=5432

ADMIN_IDS=[]
```

### 2. Создать файл `user_data.xlsx`

Разместите Excel файл с организаторами в корне проекта.

**Структура файла:**
- **Строка 1** - заголовки (будет пропущена)
- **Колонки:**
  1. ФИО ⭐ (обязательно)
  2. Подразделение ⭐ (обязательно)
  3. Телеграмм юзернейм
  4. Дата рождения (18.06.2004)
  5. Факультет
  6. Курс обучения
  7. Учебная группа (ШАЦ25-1)
  8. Номер телефона (8 (999) 123-45-67)
  9. Водительские права и машина
  10. Ближайшие станции метро
  11. Адрес проживания

### 3. Запустить

```bash
docker-compose up -d --build
```

### 4. Проверить логи

```bash
docker-compose logs -f bot
```

Вы должны увидеть:
```
Ожидание запуска PostgreSQL...
Применяем миграции...
✅ Миграции успешно применены!
Обнаружен файл user_data.xlsx, загружаем данные...
✅ Добавлено новых: X
Запуск бота...
🚀 Бот запускается...
```

---

## Команды Docker

```bash
# Запуск
docker-compose up -d

# Пересборка и запуск
docker-compose up -d --build

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f bot

# Перезапуск бота
docker-compose restart bot

# Полная очистка (включая базу данных)
docker-compose down -v
```

---

## Обновление данных организаторов

Если вы обновили файл `user_data.xlsx`:

```bash
docker-compose restart bot
```

Бот перезапустится и автоматически обновит данные из Excel файла.

---

## Что происходит при запуске (entrypoint.sh):

1. ⏳ Ожидание запуска PostgreSQL
2. 🔄 Применение миграций (`python migrate.py`)
3. 📊 Загрузка данных из `user_data.xlsx` (если файл существует)
4. 🚀 Запуск бота (`python main.py`)

---

## Полезные ссылки

- [README.md](./README.md) - подробная документация
- Структура Excel файла описана в README.md


